# v2.7.1 재고 현황 및 Excel 내보내기

생산3팀 재고 현황에서 전체 규격별 공정 재고와 실시간 부족수량을 한 화면에서 확인할 수 있도록 추가했습니다.

- 신규분류요약·품명·규격·P/Q/R 코드·공정 재고·수화 부족·완제품 부족·최우선 납기를 통합 표시합니다.
- 완제품 부족은 생산실적을 반영한 누수·규격 공정(80)의 잔여 부족량을 사용합니다.
- 수화 부족은 `MAX(0, 완제품 부족 - 누수규격검사 재고 - 검사접착 재고 - 수화 지시량)`으로 계산합니다.
- 수화 지시 API는 설정형 어댑터로 준비했으며, API 연결 전에는 임시 0으로 계산하고 미연결 상태를 표시합니다.
- 재고와 실적, 수화 지시를 같은 수집 회차에서 처리하도록 통합 갱신 구조를 추가했습니다.
- 신규분류요약 단일 선택, 수화 부족·완제품 부족 간략히 보기, 기본·납기일 정렬을 지원합니다.
- P/Q/R 코드를 표시해도 가로 스크롤 없이 화면 폭에 맞추고 잘린 값은 툴팁으로 확인할 수 있습니다.
- 모든 표에서 셀·드래그 범위·전체 선택을 Excel 호환 형식으로 복사할 수 있습니다.
- Excel은 화면 검색·간략히 보기·정렬의 영향을 받지 않고 진행현황·납기·신규분류요약 조건만 반영합니다.
- Excel의 P/Q/R 코드는 숨김 열로 포함하며, 시트 배율은 85%, 최초 선택 셀은 A9로 저장합니다.
- Excel 시트는 업무 분류 순서로 배치하고 Clear는 파란색, Color는 자주색 탭으로 구분합니다.

## 수화 지시 API

ERP 수화 지시 API가 아직 제공되지 않아 이번 버전에는 실제 지시량을 연결하지 않았습니다. API 응답을 받은 뒤 같은 시점의 ERP 목록과 제품코드별 수량 및 전체 합계를 검증하고 운영 연결합니다.

## 관리시트

구글 관리시트의 최신 배포 버전은 이번 Git 푸시에서 변경하지 않습니다. 수화 지시 API 연결과 정합성 검증이 완료된 뒤 별도로 올립니다.
