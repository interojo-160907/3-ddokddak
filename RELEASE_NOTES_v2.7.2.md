# v2.7.2 수화 지시 API 연결

ERP의 미실적 하이드레이션/전면검사 지시를 재고 및 생산실적과 같은 갱신 회차에 수집하여 수화 부족 계산에 반영합니다.

- `/api/hydration-job-list`에서 오늘을 포함한 최근 5일의 전체 공장 지시를 조회합니다.
- 제품코드별 `job_qty`를 합산하여 수화 지시량에 반영합니다.
- 수화 부족은 `MAX(0, 완제품 부족 - 누수규격검사 재고 - 검사접착 재고 - 수화 지시량)`으로 계산합니다.
- 기존 API 인증키를 재사용하며 별도 사용자 설정 없이 연결됩니다.
- 전체 건수 불일치, 잘린 응답, 생산지시 고유키 중복을 감지하면 해당 수집을 실패 처리하여 불완전한 값이 운영 계산에 섞이지 않도록 합니다.
- 원본 행과 제품별 합계를 현재 스냅샷 및 회차별 압축 스냅샷으로 보관합니다.

## 정합성 검증

2026-09-18 ERP 화면 자료와 동일 기간 API를 대조한 결과 181건, 총 지시량 327,282, 119개 제품코드가 모두 일치했습니다. CHECK NO. 및 주요 필드 차이는 0건이었습니다.

운영 기준 2026-09-14~2026-09-18 통합 수집에서도 재고 4종, 생산실적, 수화 지시 및 최종 계산이 한 회차에서 정상 완료되었습니다.
