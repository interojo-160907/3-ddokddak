# v2.6.2 수집 복구 및 공용 관리시트 보호

APS 갱신 중 발생했던 오류가 이후 정상 수집 뒤에도 남고, 15~19초 뒤 정상 응답하는 API가 6초 접속 점검에서 실패로 표시되는 문제를 보완했다.

- 접속 점검 대기를 30초로 늘리고 마지막 수집·활성 오류·WIP·접속 점검·모드 확인 결과를 상단 상태에 반영한다.
- 오류 이후의 정상 수집 시각이 확인되면 이전 오류를 현재 오류로 표시하지 않는다. 기존 자료 유지나 잘못된 시각은 복구로 인정하지 않는다.
- 전체 수집도 APS 원천 회차가 같으면 전체 재다운로드를 생략한다.
- 생산실적 일별 응답이 잘리면 최대 3회 재조회한다. 건수가 끝까지 맞지 않으면 기존 DB를 교체하지 않는다.
- 공용 관리시트의 동일 PC에 등록된 프로그램별 권한·버전·접속 상태 격리를 회귀 검증한다.
- SCM 컨트롤타워 등 다른 프로그램의 안전모드가 생산3팀 Git 자료를 가져오지 못하도록 차단한다. 다른 프로그램의 자동모드와 기존 권한·접속 처리는 유지된다.

## 관리시트 적용 순서

1. GitHub v2.6.2 설치파일 빌드가 성공한 뒤 `버전관리`에서 **생산3공장 똑딱이** 행의 `최신버전`만 `2.6.2`로 변경한다.
2. 해당 행의 설치파일 주소는 기존 `https://github.com/interojo-160907/3-ddokddak/releases/latest/download/ddokddak-production3-setup.exe`를 유지한다.
3. 기존 Apps Script 프로젝트의 `SafeMode.gs`를 이 버전 파일로 교체하고, **기존 웹 앱 배포를 새 버전으로 업데이트한다(URL 유지)**. `Code.gs`나 트리거의 재설치는 필요 없다.
4. `사용자관리`는 `PC 고유ID + 허가 프로그램`별로 한 행씩 관리한다. 동일 PC가 여러 프로그램을 쓰는 것은 정상이다. SCM 컨트롤타워 행의 권한·버전·설치파일은 별도로 유지한다.
5. `전체설정`도 프로그램별 한 행으로 관리한다. 별도 안전모드 자료 연결이 없는 다른 프로그램은 `자동모드`로 둔다. 다른 프로그램 전용 자료 연결은 해당 프로그램 구현 시 추가한다.

Google 시트 및 실제 Apps Script 배포는 이번 코드 푸시로 자동 변경되지 않는다.

## 검증

- Python 회귀 테스트 59개 통과.
- 안전모드 및 동일 PC의 다중 프로그램 권한·버전·접속 격리 Node 테스트 통과.
- 2026-09-11 최근 7일 생산실적 보완: 저장 19,794 → 19,808행, 기존 전월 이력 유지.
