# v2.6.1 실행 오류 수정

v2.6 설치판에서 openpyxl 초기화 중 `numpy.short`가 없어 종료되는 오류를 수정한다.
NumPy 2.3.1을 명시적 의존성으로 고정하고 PyInstaller 수집 대상으로 지정한다.

배포 전 EXE 자체를 실행해 GUI 모듈 로딩, Qt 렌더링, NumPy 수량의 Excel 저장/읽기,
안전모드 스냅샷 생성 및 R/Q/P 규격 연결을 검사한다. 실패하면 설치파일을 배포하지 않는다.
검사는 임시 폴더에서만 실행하며 운영 데이터와 관리 시트를 변경하지 않는다.

기존 v2.6 기능과 자료는 유지한다. 실행되지 않는 PC는 새 설치파일로 덮어 설치한다.
관리 API는 재배포할 필요가 없다. 설치파일 검증 완료 후 버전관리 최신버전을 v2.6.1로 변경한다.
