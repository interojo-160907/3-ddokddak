# 똑딱이 생산3팀 v2.8.1

v2.7.10의 복구 상태에서 재고 현황과 수화 지시 연동을 복원합니다. 앱 표시, 실행파일 및 설치파일 버전은 모두 2.8.1입니다.

- 최근 5일의 수화 지시를 제품코드별로 합산해 재고 현황과 수화 부족 계산에 반영합니다.
- 실시간 계산과 재고 현황이 같은 창고 응답을 사용하며, 일부 수집 실패나 불완전 응답에서는 마지막 정상 결과를 유지합니다.
- 요청 동시 수와 재시도를 제한하고, 실패한 창고 수집을 이어서 진행합니다.
- 첫 수집 전에 기존 데이터베이스와 설정을 `recovery/before-2.8.1`에 백업합니다.
- 실시간 데이터 갱신 후 재고 현황 및 LOT 작업순서 화면을 다시 불러옵니다.
- BOM 등록·수정 현황에 T코드와 함께 S코드의 신규등록·생산공장 변경을 표시합니다. 보관된 비교 이력에서 누락된 S코드를 보완하고 기존 T코드 이력은 중복하지 않습니다.

2026-09-23 실제 API 15개 조회에서 HTTP 200과 전체 행 수 일치를 확인했습니다. 수화 지시 70건(46개 제품, 합계 120,702개)을 정상 집계했습니다. 분리된 데이터 폴더의 실제 통합 수집도 198.74초에 성공했습니다(APS 42,227행, 재고 계산 81개 제품·11,617행). Python 회귀 검사 110개, 관리 기능 Node 검사 2개, 소스 실행의 Qt 렌더링·수집 실패 복구 시뮬레이션을 통과했습니다. 이 결과는 해당 시점의 검사이며 ERP 화면과 제품별 수량을 직접 대조한 결과는 아닙니다.

이 설치파일은 코드 서명 없이 빌드됩니다. 이전 v2.7.10에서 확인된 Smart App Control 설치 차단의 해결을 의미하지 않습니다. 배포 워크플로는 기존 버전별 설치·데이터 보존 및 v2.7.10 복구 검사가 성공한 후 설치파일을 게시합니다.
