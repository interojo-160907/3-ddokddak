# v2.7.9 API 수집 지연 대응

ERP 응답이 늦을 때 대기와 재시도가 누적되던 부분을 줄였습니다. 수화 지시는 기존처럼 오늘을 포함한 5일 전체 목록을 조회하며, 창고는 순서대로 수집합니다.

- 모든 ERP 수집기가 환경변수와 Windows 자격 증명 저장소의 동일한 API 키를 사용합니다. 빈 별칭 때문에 다른 유효 키를 놓치지 않습니다.
- 스레드별 HTTP 연결을 재사용하고, 한 수집 프로세스의 동시 ERP 요청은 최대 2건으로 제한합니다. TLS 인증서 검증은 유지합니다.
- 연결 대기는 5초, 수화·재고·규격의 응답 대기는 30초, 실적/WIP는 최대 45초, APS/BOM은 최대 60초입니다. 이 값은 소켓 연결/읽기 비활성 제한이며 전체 수집 완료시간 보장은 아닙니다.
- 응답 시간 초과에는 같은 요청을 즉시 반복하지 않습니다. 다음 자동 갱신에서 재시도합니다. 연결 오류와 일시적인 HTTP 오류만 한 번 더 시도하며, 인증·요청 조건 오류는 반복하지 않습니다. 서버의 긴 Retry-After 안내는 다음 회차로 넘깁니다.
- 규격·일별 실적 조회가 실패하면 대기 중인 수백 건을 끝까지 호출하지 않습니다. 성공한 규격은 저장해 다음 회차에 누락분만 받습니다.
- 접속 점검을 매분에서 5분 주기로 줄이고, 점검 응답 대기는 8초로 제한했습니다. 수집 진행 중 새 점검을 시작하지 않습니다.
- 수화 응답의 잘못된 수량·중복·누락은 기존 정상 스냅샷을 덮어쓰지 않습니다. 정상 0건은 실제 0으로 반영합니다. 미완료 수집은 기존 계산 결과를 유지합니다.
- API 오류에 요청 경로·오류 종류·경과 시간·시도 횟수를 남겨 지연 구간을 확인할 수 있습니다. 인증키와 원문 오류 응답은 출력하지 않습니다.

## 검증

- Python 회귀 테스트 106개 및 중앙 관리 JavaScript 테스트 2개 통과.
- 실제 로컬 HTTP 서버로 연결 재사용·JSON 수신을 검증했습니다.
- 수집 시뮬레이션: 최초 데이터 생성, 창고 실패 후 복구, 재고 0건, 이전 계산 보존, 수화 부족 계산, 실제 재고 표 렌더링.
- 배포 워크플로가 v2.6.4 이상 모든 기존 공개 설치본에서 새 설치본으로 직접 업데이트하고, 데이터 경로·사용자 파일 보존 및 패키지 실행 시뮬레이션을 통과해야 설치파일을 게시합니다.

## 실 API 확인과 배포 판단

2026-09-19 07:45 KST 분리된 임시 폴더에서 확인:

- `/api/health`: 약 3.47초, 정상 응답.
- `/api/hydration-job-list`: 오늘 포함 5일, 30.03초 후 응답 대기 시간 초과. 운영 데이터 변경 없음.
- 메일 예시(C관 9/1~9/18) 및 오늘 S관 1건 요청도 이전 진단에서 각각 약 20초 후 응답 대기 시간 초과였습니다. 조회량 축소만으로 개선되는 것은 확인하지 못했습니다.
- 서버 상태의 `auth.require_api_key`는 `false`였습니다. 따라서 키 처리 불일치를 지금의 실 API 지연 원인으로 단정하지 않습니다. 이전의 인증키 없는 APS 점검만으로 서버 장애를 확정할 수도 없습니다.
- 이번 변경은 클라이언트의 불필요한 대기·요청을 줄인 것입니다. ERP 데이터 조회 자체의 응답 지연이 해소됐다는 의미가 아닙니다. 실제 수화 수집 및 재고 표 정상 갱신 확인 전 Google Sheet 배포 버전 변경은 보류합니다.

전송 방식 참고: [Requests 공식 문서 — 연결 재사용과 제한 시간](https://requests.readthedocs.io/en/latest/user/advanced/).
