# v2.6 — 안전모드

- 전체설정 A열 `생산3공장 똑딱이`의 C열을 15초마다 확인한다.
- 중앙 관리 API가 안전모드 최초 조회 시 Git 최신 날짜/오전·오후 파일의 커밋을 고정한다.
- `안전모드_APS자료`의 엑셀을 로컬 `안전모드` 아래 원본·DB·스냅샷으로 분리한다.
- 대시보드·공정현황 APS는 안전모드 자료를 사용한다. 자동수집 저장소는 변경하지 않는다.
- 실시간 실적 반영·LOT 작업순서 및 내부탭을 비활성화한다. BOM과 최신 생산실적 조회는 유지한다.
- 자동모드 복귀 시 기존 APS·실시간 계산 데이터를 확인하고 조회 경로 복원 후 안전모드 자료를 정리한다.
  기존 정상운영의 WIP 대기 상태는 그대로 표시하며 이전 계산을 최신 회차로 표시하지 않는다.
- 작업표시줄 아이콘의 16~256px 크기를 포함하고 Win32의 작은/큰 아이콘 슬롯을 설정한다.
- 미리보기는 기존 창을 유지하며 자동수집 중복 실행을 막는다. 설치판의 스케줄 수집 동작은 유지한다.

## 입력 제한

현재 Excel은 공정 제품코드와 수량만 제공한다. 판매 T코드, 주문총수량, 계획일별 상세,
수주 비고를 추측해 보충하지 않는다. WIP·실시간 계산 자료로 사용하지 않는다.
R/Q/P 공정코드는 수주·이니셜·거래처·분류·수요품명·납기·광학 규격(도수/CP/AXIS/ADD)이 같은 경우 한 행으로 연결한다. 원본 공정코드는 별도 보존하며 다른 축·도수·ADD·수주·품명·납기를 섞지 않는다.

## 배포 전 필요 작업

1. 관리 Apps Script에 `Code.gs` 변경과 `SafeMode.gs`를 함께 반영하고 기존 웹앱 배포를 새 버전으로 갱신한다. `setupSafeModeTrigger`를 한 번 실행해 C열 편집 시 고정 파일을 초기화하는 트리거를 등록한다.
2. Git에 코드와 `안전모드_APS자료/260907_오전.xlsx`를 푸시한다.
3. 실제 전체설정 C열 전환을 설치판에서 검증한다. 현재 로컬 검토에서는 시험 설정 파일을 사용한다.

관리 API의 운영 배포는 별도 단계이다. 코드/설치파일 업데이트만으로 전체설정 모드 기능이 활성화되지는 않는다. API 반영 전 C열은 자동모드로 유지한다.

## 로컬 검증

- Python 회귀 테스트 55개 통과.
- 관리 API 모의 시험: 프로그램 행 선택, 최신 날짜 선택, PC 공통 커밋 고정, 자동모드 초기화 통과.
- 실제 260907_오전.xlsx: S관 4,733행 → 공정 레코드 7,783행. 원본과 화면의 공정 합계 일치.
- 원본 2.33MB + 변환 DB 8.77MB + 스냅샷 약 0.5KB.
- 별도 창에서 안전→자동 전환, 12개 메뉴·내부탭 재활성화, 안전모드 자료 삭제 확인.
- Windows 작은/큰 아이콘 핸들 설정 확인.

- 공정코드 분리 표시 버그 수정: R202608110001 / +03.75 / -1.25 / 축010을 1,443 / 1,443 / 1,067 / 1,065 / 1,008로 단일 행 표시. 전체 공정 합계 불변 확인. 스냅샷 변환 버전 2로 기존 캐시 재생성.

- 변환 버전 3: 동일 수주·수요품명·분류·납기에서 광학 규격으로 연결하여 공정별 색상 코드 별칭 차이를 허용. PIA_BROWN MANEGE -04.00 Q/RHA2와 P/CRG3 연결 확인. 표시 2,546→2,523행, 전체 공정 합계 불변. Nocturne -04.00은 원본 Q 1,227만 있어 그대로 유지.

## 업데이트 및 보관 위치

- 설치/업데이트와 앱 시작 시 선택한 데이터 저장소 아래 `안전모드` 폴더를 자동 생성한다. 기본 위치는 `C:\똑딱이 생산3팀 API DATA\안전모드`이며 사용자 지정 저장소를 우선한다.
- 파일별 해시 폴더에 원본 Excel, aps.sqlite, 스냅샷.json을 보관하고 상위 적용정보.json에 적용 차수를 기록한다. 앱 재시작/업데이트 시 적용 상태를 유지한다.
- 다운로드 실패 시 기존 적용본 유지, 손상된 변환 캐시 재생성, 상태 파일 삭제 실패 시 안전모드 유지 회귀 시험 통과.
- openpyxl 및 안전모드 모듈을 설치파일에 포함한다. 설치 시 기존 수집자료를 덮어쓰지 않는다.
- 중앙 관리 API 배포와 setupSafeModeTrigger 실행을 마친 후 각 PC를 v2.6으로 업데이트한다. 그 다음 전체설정 C열을 안전모드로 바꾸고 헤더의 2026-09-07 오전 표시를 확인한다.

- 푸시된 v2.6 커밋의 Git 원본을 다운로드하여 해시 검증 및 재변환: 표시 2,523행, 5개 공정 총합 일치. 원본 2,333,384바이트 + DB 8,765,440바이트 + 스냅샷 536바이트(총 약 11.10MB).
