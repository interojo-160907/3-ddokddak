# v2.7.8 창고 순차 수집·업데이트 복구 안정화

2.6.4부터 사용하던 PC에서 재고 스냅샷이 없어도 필요한 폴더와 수화 설정을 만들고 최초 수집을 시작합니다. 창고별 정상 응답을 저장하며, 실패한 항목을 재시도해 재고 현황을 자동으로 표시합니다. 기존 사용자 데이터 위치와 설정을 유지합니다.

- 사출창고 → 분리창고 → 검사접착 → 누수규격검사 → 내부 계산용 규격검사완료 순서로 수집합니다. 표시용 재고와 실시간 계산의 중복 API 요청을 제거했습니다.
- 같은 조회 기간의 실패 회차를 복구할 때는 15분 이내 정상 응답을 재사용하고, 실패한 창고만 재시도합니다. 실제 수집 시각은 유지합니다. 정상 회차 이후의 수동/자동 갱신은 모든 창고를 새로 수집합니다.
- 정상 0건은 빈 재고로 저장합니다. 오류·잘린 응답·행 수 불일치는 0건으로 취급하지 않습니다. 입력 수집이 불완전하면 마지막 정상 계산/조회 결과를 유지하며 다른 필터의 결과를 대신 표시하지 않습니다.
- SQLite 읽기 연결을 즉시 닫아 Windows 파일 교체 잠금을 해소했습니다. 수집 완료 후 실시간·LOT·재고 화면이 새 스냅샷을 반영합니다.
- 수집 중인 창고와 완료 수를 표시합니다. 중단과 오류를 구분하고, 사용자가 중단하면 자동 수집을 5분 유예합니다. 일부 재고 실패 때문에 BOM·APS·생산실적 전체를 반복 수집하던 경로도 수정했습니다.
- 최초 재고 조회는 API를 화면 스레드에서 호출하지 않습니다. 규격 조회/계산은 수집 프로세스에서 수행하며, 화면은 검증된 스냅샷을 사용합니다.

## 검증

- Python 회귀 테스트 90개와 중앙 모드/권한 Node 테스트 2개 통과.
- 구버전과 동일한 APS 스키마/기존 사용자 파일을 가진 격리 폴더에서 첫 수집 실패 → 창고별 복구 → 재고표 렌더링까지 실제 수집·SQLite·UI 코드를 실행했습니다.
- 정상 0건, 잘린 응답 거부, 실패 시 DB/조회 결과 유지, 필터 일치, Windows 파일 즉시 교체, 연속 갱신, 수화 부족 산식을 검증했습니다.
- 배포 실행파일 검사에도 같은 오프라인 시뮬레이션을 포함했습니다. API 응답은 시뮬레이션 데이터이며 실제 다른 PC 설치를 직접 검증했다는 의미는 아닙니다.
- 2026-09-19 실제 API 점검 중 수화 지시(120초) 및 APS 메타(15초) 응답 지연을 확인했습니다. 서버/망 응답 시간은 프로그램이 보장할 수 없으며, 오류 상태를 유지하고 재시도합니다. 실제 API 전체 수집 완료는 별도 확인이 필요합니다.

구글시트의 배포 버전은 설치파일 검사 완료 후 변경합니다. 파일명은 `ddokddak-production3-setup.exe`를 유지합니다.
